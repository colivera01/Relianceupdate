'use client';

/**
 * Admin profile placeholder. Full profile editor can replace this later.
 */
export default function Profile() {
  return (
    <div className="container mx-auto max-w-3xl space-y-3 p-6">
      <h1 className="text-2xl font-bold text-gray-900">Admin Profile</h1>
      <p className="text-sm text-gray-600">
        Profile settings UI is not wired here yet. This placeholder keeps the admin route buildable.
      </p>
    </div>
  );
}
